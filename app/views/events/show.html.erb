<h1><%= @event.title %> by <%= @creator.name %></h1>

<p> <%= @event.description %> </p>
<p>		<%= @event.date.strftime("%B %d, %Y") %> at <%= @event.date.strftime("%I:%M %p") %>
</p>

<h3> Attendees </h3>
<% @attendees.each do |attendee| %>
	<%= attendee.name %>
<% end %>

